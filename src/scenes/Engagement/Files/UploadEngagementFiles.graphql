mutation UploadLanguageEngagementPnp(
  $id: ID!
  $upload: CreateDefinedFileVersionInput!
) {
  updateLanguageEngagement(input: { engagement: { id: $id, pnp: $upload } }) {
    engagement {
      id
      pnp {
        value {
          ...FileNodeInfo
          children {
            items {
              ...FileNodeInfo
            }
          }
        }
      }
    }
  }
}

mutation UploadInternshipEngagementGrowthPlan(
  $id: ID!
  $upload: CreateDefinedFileVersionInput!
) {
  updateInternshipEngagement(
    input: { engagement: { id: $id, growthPlan: $upload } }
  ) {
    engagement {
      id
      growthPlan {
        value {
          ...FileNodeInfo
          children {
            items {
              ...FileNodeInfo
            }
          }
        }
      }
    }
  }
}
