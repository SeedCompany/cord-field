mutation CreateStory($input: CreateStory!) {
  createStory(input: $input) {
    story {
      id
      name {
        value
      }
    }
  }
}
query Stories {
  stories {
    items {
      id
      name {
        value
      }
    }
  }
}
