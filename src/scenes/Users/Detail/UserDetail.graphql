query User($userId: ID!) {
  user(id: $userId) {
    ...userDetails
  }
}

fragment userDetails on User {
  id
  email {
    value
  }
  fullName
  bio {
    value
  }
  phone {
    value
  }
  timezone {
    value {
      name
    }
  }
  createdAt
}
