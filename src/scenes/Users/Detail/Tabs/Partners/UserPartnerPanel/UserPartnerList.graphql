query UserPartners($userId: ID!, $input: PartnerListInput) {
  user(id: $userId) {
    id
    partners(input: $input) {
      canRead
      hasMore
      total
      items {
        ...partnerDataGridRow
      }
    }
  }
}
