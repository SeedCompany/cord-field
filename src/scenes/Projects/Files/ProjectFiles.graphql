query ProjectRootDirectory($id: ID!) {
  project(id: $id) {
    ...ProjectBreadcrumb
    rootDirectory {
      id
    }
  }
}

query ProjectDirectory($id: ID!) {
  directory(id: $id) {
    ...FileNodeInfo
    children {
      total
      items {
        ...FileNodeInfo
        ... on Directory {
          ...FileNodeInfo
        }
        ... on File {
          ...FileNodeInfo
          ...FileInfo
          size
          mimeType
        }
        ... on FileVersion {
          ...FileNodeInfo
          size
        }
      }
    }
    parents {
      id
      name
    }
  }
}
