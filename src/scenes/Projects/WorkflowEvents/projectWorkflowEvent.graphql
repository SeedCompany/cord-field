fragment projectWorkflowEvent on ProjectWorkflowEvent {
  id
  at
  to
  who {
    value {
      ... on User {
        id
        fullName
      }
      ... on SystemAgent {
        id
        name
      }
    }
  }
  transition {
    to
  }
}
