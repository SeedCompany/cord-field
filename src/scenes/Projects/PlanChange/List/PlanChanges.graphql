query PlanChanges($project: ID!, $input: ChangeListInput) {
  project(id: $project) {
    ...ProjectBreadcrumb
    changes(input: $input) {
      items {
        ...PlanChangeCard
      }
      ...Pagination
      canRead
      canCreate
    }
  }
}
