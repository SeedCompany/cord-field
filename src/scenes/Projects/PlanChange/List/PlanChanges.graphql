query PlanChanges($project: ID!, $input: PlanChangeListInput) {
  project(id: $project) {
    ...ProjectBreadcrumb
    planChanges(input: $input) {
      items {
        ...PlanChangeCard
      }
      ...Pagination
      canRead
      canCreate
    }
    status
  }
}
