query PlanChanges($project: ID!, $input: PlanChangeListInput) {
  project(id: $project) {
    ...ProjectBreadcrumb
    changes(input: $input) {
      items {
        ...PlanChangeCard
      }
      ...Pagination
      canRead
      canCreate
    }
  }
}
