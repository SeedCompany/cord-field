fragment LanguageEngagementReport on LanguageEngagement {
  id
  currentProgressReportDue {
    ...SecuredPeriodicReport
  }
}

query EngagementReport($engagementId: ID!) {
  engagement(id: $engagementId) {
    id
    ...LanguageEngagementReport
  }
}
