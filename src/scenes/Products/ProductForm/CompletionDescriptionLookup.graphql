query CompletionDescriptionLookup(
  $query: String
  $methodology: ProductMethodology
) {
  suggestProductCompletionDescriptions(
    input: { query: $query, methodology: $methodology }
  ) {
    items
  }
}
