mutation UpdateProductProgress($input: ProductProgressInput!) {
  updateProductProgress(input: $input) {
    product {
      id
    }
    steps {
      step
      percentDone {
        value
      }
    }
  }
}
