mutation CreatePartner($input: CreatePartner!) {
  createPartner(input: $input) {
    partner {
      id
      organization {
        value {
          name {
            value
          }
        }
      }
      ...PartnerListItem
      ...PartnerLookupItem
    }
  }
}
