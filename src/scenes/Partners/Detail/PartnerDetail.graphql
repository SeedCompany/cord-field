query Partner($input: ID!) {
  partner(id: $input) {
    ...PartnerDetails
  }
}

fragment PartnerDetails on Partner {
  id
  createdAt
  organization {
    canRead
    canEdit
    value {
      name {
        ...securedString
      }
      avatarLetters
    }
  }
}
