query PrayerStepList($engagement: ID!, $input: PostListInput) {
  engagement(id: $engagement) {
    ... on LanguageEngagement {
      id
      ...PostableId
      posts(input: $input) {
        canRead
        canCreate
        items {
          id
          type
          shareability
          effectiveShareability
          approvedShareability {
            value
            canEdit
          }
          body {
            value
          }
          finalBody {
            value
            canEdit
          }
          featured {
            value
            canEdit
          }
          report {
            canRead
            value {
              id
              start
              end
            }
          }
          ...PostForm
        }
      }
    }
  }
}

mutation AttachPrayerToReport($input: UpdatePost!) {
  updatePost(input: $input) {
    post {
      id
      report {
        value {
          id
        }
      }
    }
  }
}

mutation ModeratePrayerRequest($input: ModeratePost!) {
  moderatePost(input: $input) {
    posts {
      id
      approvedShareability {
        value
        canEdit
      }
      effectiveShareability
    }
  }
}

mutation UpdatePrayerFinalWording($input: UpdatePost!) {
  updatePost(input: $input) {
    post {
      id
      finalBody {
        value
        canEdit
      }
    }
  }
}

mutation ToggleFeaturedForInvestorReport($input: UpdatePost!) {
  updatePost(input: $input) {
    post {
      id
      featured {
        value
        canEdit
      }
    }
  }
}
