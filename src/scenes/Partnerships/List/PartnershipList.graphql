query ProjectPartnerships($project: ID!, $input: PartnershipListInput) {
  project(id: $project) {
    ...ProjectBreadcrumb
    partnerships(input: $input) {
      canCreate
      items {
        ...PartnershipCard
        ...PartnershipForm
      }
      ...Pagination
    }
  }
}
