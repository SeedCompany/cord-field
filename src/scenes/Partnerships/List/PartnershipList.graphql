query ProjectPartnerships($input: ID!) {
  project(id: $input) {
    ...ProjectDetailNavigation
    partnerships {
      canCreate
      total
      items {
        ...PartnershipCard
        ...PartnershipForm
      }
    }
  }
}
