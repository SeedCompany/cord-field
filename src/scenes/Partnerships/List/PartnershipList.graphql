query ProjectPartnerships($input: ID!) {
  project(id: $input) {
    id
    name {
      value
    }
    partnerships {
      canCreate
      total
      items {
        ...PartnershipCard
        ...EditPartnership
      }
    }
  }
}
