query FileDownloadUrl($id: ID!) {
  file(id: $id) {
    id
    downloadUrl
    name
  }
}

mutation RenameFileNode($input: RenameFileInput!) {
  renameFileNode(input: $input) {
    id
    name
  }
}

mutation DeleteFileNode($id: ID!) {
  deleteFileNode(id: $id)
}
