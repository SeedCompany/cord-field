query ChangesetDiffBanner($changeset: ID!) {
  changeset(id: $changeset) {
    id
    ...ProjectChangeRequestListItem
    ... on ProjectChangeRequest {
      project {
        ...Id
      }
    }
  }
}
