query ProjectChangesetDiffs($id: ID!, $changeset: ID) {
  project(id: $id, changeset: $changeset) {
    ...Id
    changesetDiff {
      ...ChangesetDiff
    }
    changeset {
      ...ProjectChangeRequestListItem
    }
  }
}
