query LocationLookup($query: String!) {
  search(input: { query: $query, type: [Location] }) {
    items {
      ...LocationLookupItem
    }
  }
}

fragment LocationLookupItem on Location {
  id
  name {
    value
  }
}
