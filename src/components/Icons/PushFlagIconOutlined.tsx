import FlagOutlinedIcon from '@material-ui/icons/FlagOutlined';
import { SvgIcon, SvgIconProps } from '@material-ui/core';
import * as React from 'react';
import { FC } from 'react';

export const PushFlagIconOutlined: FC = (props) => (
  <div>
    <FlagOutlinedIcon />
  </div>
);
