import FlagIcon from '@material-ui/icons/Flag';
import { SvgIcon, SvgIconProps } from '@material-ui/core';
import * as React from 'react';
import { FC } from 'react';

export const PushFlagIconFilled: FC = (props) => (
  <div>
    <FlagIcon />
  </div>
);
