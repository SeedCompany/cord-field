mutation CreateToolUsage($input: CreateToolUsage!) {
  createToolUsage(input: $input) {
    toolUsage {
      ...ToolUsageForm
      # return entire list of tools for container cache
      container {
        __typename
        id
        tools {
          id
        }
      }
    }
  }
}
