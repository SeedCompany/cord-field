mutation CreateOrReplyComment($input: CreateCommentInput!) {
  createComment(input: $input) {
    comment {
      ...CommentProps
    }
    commentThread {
      id
      ...CommentThreadProps
    }
  }
}
