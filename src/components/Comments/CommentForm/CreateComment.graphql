mutation createComment($input: CreateComment!) {
  createComment(input: $input) {
    comment {
      ...comment
    }
    commentThread {
      id
      ...commentThread
      container {
        __typename
        id
      }
    }
  }
}
